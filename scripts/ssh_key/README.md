## SSH-ключи: создание и распространение
- **-** **`ssh_key.sh`** - это скрипт, который создаёт и распространяет ssh-ключ по протоколу ssh.

   ### Внимание!!!
  Для работы нужен список с ip.

  ### Активация скрипта
  ```
  bash ssh_key.sh
  ```
  
